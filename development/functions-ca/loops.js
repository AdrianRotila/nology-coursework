// 1. Create coaches array
const coaches = ["Andy", "Sam", "Bex"];
console.log(coaches[1]);

// 2. Create an array of pets
const pets = ["Rex", "Bobita", "Mircea"];
console.log(pets[2]);

// 3. Create a sample for loop
for (let index = 0; index < 5; index++) {
    console.log("The index on this loop was " + index);
}

// 4. Loop through an array
for (let index = 0; index < coaches.length; index++){
    console.log(coaches[index]);
}